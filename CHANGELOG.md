# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)，格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [1.1.0] - 2026-09-26

### 变更

- **导出文件名改为 `歌曲名-歌手`**（原来是 `歌曲名`），如 `人是猫-张卡斯.mp3`；读不到歌手信息时仍只用歌曲名，
  读不到歌曲名时仍用时间戳。重名自动加 `(2)` 的规则不变。
  **ID3 标签不受影响**：标题仍是纯歌曲名，歌手/专辑照旧单独写入——只有文件名带上歌手，便于在文件管理器里区分同名歌曲。
  拼装逻辑集中在 `RecorderEngine.BuildFileBase(title, artist)`。

### 新增

- **自动录制（单首）**：新增「自动录制」勾选框。勾选后，只要在播放器里**开始播放**（点播放 / 下一曲播放），
  就会自动把歌曲倒回开头并开始录制这一首，无需再点「开始录制」；勾选时若已经在播放，会立即开始录制。
  判定用「播放上升沿」——一次播放只触发一次，播放停止/暂停后重新武装，因此录制结束时自动暂停播放器
  不会被误判成新的播放而反复开录。**该模式不追踪换歌**，一次播放只录一首。
  自动模式下不弹任何对话框（失败只写状态栏与日志），避免打断听歌。
- **录制时长过短时询问是否导出**：录制因**被手动暂停或打断**结束（手动停止 / 播放器被暂停停止 / 采集中断）、
  且录制时长不足 **10 秒**时，弹窗提示「录制时长过短，音频可能被手动暂停或打断，是否仍要导出这段音频？」。
  选「是」正常导出；选「否」直接删除已生成的音频文件、不更新「上次导出」。
  正常播完（曲末 / 换歌）结束的短音频不会询问。
- **自动录制整张歌单**：新增独立勾选框。勾选后，点播放开始录制，**当前歌曲放完后自动接着录下一首**，
  每首歌单独导出成一个 MP3；连录期间不会在每首结束时暂停播放器（否则无法连录），播放停止或歌单放完即结束。
  与「自动录制（单首）」相互独立，可同时勾选（整张歌单是单首的超集）。
  换歌触发时会**强制把新歌倒回 0:00**（`RecorderOptions.ForceRestart`）——新歌此时已播放了一小段，
  靠"位置判据"会误判成"已在开头"，不强制重定位就会漏掉开头。
- **界面调整：所有勾选框集中到独立的「② 录制选项」项目框**，放在「开始录制」按钮上方；
  上方选项区只保留目标播放器 / 录音设备 / MP3 码率。窗口高度相应从 610 调整为 760。
- **顶部免责声明**：窗口最上方以红色大字提示「本项目仅供学习与技术交流使用，严禁任何形式的商业用途」，
  并附一行说明（录制的音频仅限个人合法使用、请勿传播售卖、遵守平台版权与会员协议）。
- 自检工具 `--pause` 支持 `--app=<媒体会话标识>`，可精确暂停指定播放器（同时开着多个播放器时便于自检）。
- **模拟播放器自检工具支持多首连播**：`--fakeplayer --files="a.mp3;b.mp3" --titles="一;二"`，
  用 `MediaPlaybackList` + 逐项显示属性注册歌曲信息（歌曲名会被系统媒体会话正确上报），用于端到端验证"歌单连录"。

### 修复

- **修复「目标播放器」只在开始录制那一刻生效**：平时刷新会话（界面显示的来源、以及「自动录制」的判定）
  走的是"自动挑选"逻辑，用户在下拉框里选定的播放器不参与挑选；当用户选定的播放器不被内置播放器名单识别时，
  会出现"界面盯着另一个播放器、自动录制不触发"的问题。现在 `MediaSessionService.PreferredAppId` 一经改动
  即让下一次刷新重新挑选会话，界面监控与自动录制都遵守用户的选择。
- **修复目标播放器选择被列表刷新抹掉**：会话列表刷新会重建下拉框，用户选定的播放器若当时没在运行，
  选中项会回落到「自动选择」并覆盖用户保存的选择；现在程序化刷新不会再改写用户选择。
- **修复「导出路径」输入框里只显示几个白点、看不到路径文字**：自定义 `TextBox` 模板在 `PART_ContentHost`
  上又套了一次 `Padding`（`Margin="{TemplateBinding Padding}"`），而内容宿主本身已经消费了 `TextBox.Padding`，
  于是文本行的可用高度被算成「34 − 边框 2 − Padding 7×2 − 重复的 Padding 7×2 = **4 DIP**」，
  13 DIP 的文字（实际绘制高度 17 DIP）被裁到只剩顶部几个像素——看起来就是一排小白点。
  现改为与系统默认模板一致的结构：模板内不再处理 `Padding`，垂直居中交给 `VerticalContentAlignment="Center"`，
  并给 `PART_ContentHost` 补上标准的 `Focusable="False"` 与滚动条隐藏设置。
  已在 150% 缩放（144 DPI）下用最小复现探针确认根因并验证修复（修复后 viewport/文本高度均为 18 DIP，文字完整显示）。

## [1.1.1] - 2026-09-27

### 新增

- **32 位（x86）版本**：新增 32 位安装包 `MusicRecorder-v1.1.1-win-x86.zip`（自包含单文件，64.6 MB），
  供 32 位 Windows / 老设备使用；功能与 64 位版完全一致，目标机器无需安装 .NET 或任何依赖。
  - 构建：`build.ps1` 支持 `-RuntimeIdentifier win-x86`（默认仍为 `win-x64`），
    `MusicRecorder.csproj` 的 `<Platforms>` 由 `x64` 改为 `x64;x86`。
  - 32 位包实测：PE 头 `0x014C`，包内 `--selftest` 显示 `进程: x86`，
    SMTC / 设备检查、WASAPI 环回录制、LAME 编码与 MP3 解码校验全部通过。

### 变更

- 版本号来源同步：`MusicRecorder.csproj` 的 `<Version>` 与 `app.manifest` 的 `assemblyIdentity` 均升至 1.1.1；
  `使用说明.txt` 系统要求改为「64 位与 32 位（x86）系统均支持」。

### 修复

- **修复 `build.ps1` 在 Windows PowerShell 5.1 下无法执行**：脚本原为无 BOM 的 UTF-8，
  被按 ANSI/GBK 解码时中文字符串被拆断，报 `UnexpectedToken` 解析错误；已补 UTF-8 BOM，
  PowerShell 7 与 Windows PowerShell 5.1 均可正常执行。

## [1.0.0] - 2026-09-23

首个可用版本：在 Windows 上自动识别音乐软件正在播放的歌曲，从头播放并内录系统声音，结束后按歌曲名导出 MP3。

### 新增

- **歌曲识别**：通过 Windows 系统媒体会话（SMTC / WinRT）读取播放器的歌曲名、歌手、专辑、播放进度与播放状态；
  无媒体会话时用播放器窗口标题兜底识别。
- **自动从头播放**：三重方案——系统媒体控制定位 →「下一曲 + 上一曲」→ 系统媒体键，并按播放器**声明的能力**选择与校验。
- **系统声音内录**：WASAPI 环回（Loopback）采集，32bit float → 16bit PCM → LAME 直接编码 MP3（含 ID3 标签），
  LAME 不可用时自动退化为「先录 WAV 再用 Windows Media Foundation 转 MP3」。
- **自动停止**：曲末 / 换歌 / 播放状态离开播放中 / 进度回退 / 20 分钟超长保护。
- **按歌曲名导出**：文件名＝歌曲名，重名自动加 `(2)`，写入标题/歌手/专辑标签。
- **结束后自动暂停播放**：自动停止时若播放器已切到下一首，会在换歌瞬间立刻暂停。
- **界面**：上方导出路径（浏览 / 打开目录），中部歌曲信息与进度、电平、已录制时长，
  下方大号「开始录制 / 停止并保存」按钮；可选目标播放器、录音设备、MP3 码率。
- **设置持久化**：`%APPDATA%\MusicRecorder\settings.json`；日志写 `%LOCALAPPDATA%\MusicRecorder\logs\`。
- **命令行自检/诊断**：`--selftest`、`--e2e`、`--diag`、`--compare`、`--gentest`、`--fakeplayer`、`--pause`。

### 修复

- 修复录音被静音填充"跑飞"的问题：`BufferedWaveProvider` 的 `ReadFully` 会导致音频泵以远快于实时的速度写入静音，
  6 秒录制产出 35 分钟 MP3（改为按真实时间补静音）。
- 修复高 DPI / 小屏下窗口超出屏幕可用区、录制按钮被挤出可见区域的问题。
- 修复播放器不提供歌曲名时直接拒绝开始录制的问题（改为仍可录制并以时间戳命名 + 提示）。
- 修复下拉框显示为对象字符串的问题（`DisplayMemberPath` 与自定义模板下的显示）。
- 修复录制结束后播放器继续播放下一首的问题（新增自动暂停，并在换歌瞬间立即暂停）。
- **修复 QQ音乐 不会从头播放**：QQ音乐 的 SMTC 时间轴恒为 0，且 `TryChangePlaybackPositionAsync(0)` 返回 True 却无任何效果，
  旧逻辑据此误判"歌曲已在开头"而跳过倒回；现改为以播放器能力声明为准，并用「下一曲 + 上一曲」确保回到 0:00。
- 修复暂停状态下无法起播的问题（播放指令失败时用系统媒体键兜底）。

### 已知限制

- QQ音乐 / 网易云音乐 的媒体会话**不提供播放进度与总时长**（QQ音乐 窗口为自绘 UI，UI 自动化树为空），
  因此这两家无法显示百分比进度条，结尾判定依赖"换歌 / 停止播放"；界面改为显示已录制时长（＝歌曲进度）。
- 录制内容为系统总输出，录制期间的其他声音也会被录入。
